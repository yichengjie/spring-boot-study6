1. 虚拟机再OOM异常出现后自动生成dump文件
    ```text
    -XX:+HeapDumpOnOutOfMemoryError
    ```
2. 查看类加载参数
    ```text
    -XX:+TraceClassLoading
    ```
3. 永久代（方法区）参数
    ```text
    -XX:PermSize=64M
    -XX:MaxPermSize=128M
    ```
    